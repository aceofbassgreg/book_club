<td class="pt-4">
<label class="checkbox">
  <input type="checkbox">
  Add to new poll
</label>
</td>
<td class="pt-4">
  <%= link_to(
    book_path(book),
    method: :delete,
    data: {
      confirm: "#{book.title} will be removed. Are you sure?"
    },
    class: "button is-danger is-small is-rounded"
  ) do %>
    <span class="icon is-large">
      <i class="fas fa-times"></i>
    </span>
    <span>Delete book</span>
  <% end %>
</td>
